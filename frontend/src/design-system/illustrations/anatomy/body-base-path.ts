// src/design-system/illustrations/anatomy/body-base-path.ts
// ─────────────────────────────────────────────────────────────────────────────
// هندسهٔ مرجعِ پیکرهٔ انسان — نیمهٔ راستِ یک سیلوئتِ واقعی، و آینهٔ آن.
//
// مرجع: Wikimedia Commons «Human body silhouette.svg»
// (Mikael Häggström / RexxS — Public Domain)، در بوم ۴۰۰×۶۰۰.
//
// چرا این‌شکلی:
//   خودِ آن سیلوئت آناتومیِ درستی دارد (انگشتان، مچ، دلتوئید، قوسِ ساق، کفِ پا)
//   ولی چون از تصویری دست‌کشیده وکتور شده، دو نیمه‌اش دقیقاً قرینه نبودند و
//   «وارپِ» چاق/لاغر آن اختلاف را بزرگ‌نمایی می‌کرد؛ دست و پا کج می‌شد.
//
//   پس یک‌بار به‌صورت آفلاین: سیلوئت رستر شد، محورِ تقارنش پیدا شد (۲۰۰٫۷)،
//   نیمهٔ راستش استخراج و مرزش دنبال شد، و نتیجه به این جدولِ نقطه‌ها تبدیل
//   گشت. حالا نیمهٔ چپ، آینهٔ ریاضیِ همین نقاط است — پس تقارن ذاتی است، ولی
//   جزئیاتِ انسانیِ اصل هم سرِ جایش مانده.
//
// خروجی برای هر جنسیت یک‌بار ساخته و کش می‌شود.
// ─────────────────────────────────────────────────────────────────────────────

export type BodySex = "male" | "female";

/** گام نمونه‌برداریِ جدولِ نیم‌پهنای تنه (پیکسل). */
export const CORE_HALF_STEP = 4;

/** محورِ تقارنِ بوم مرجع 400×600. */
const CENTER_X = 200;

export interface BaseOutline {
  /** مسیرِ بستهٔ سیلوئت در بوم 400×600. */
  readonly path: string;
  /** نیم‌پهنای «تنه» (بدون بازوها) در هر ارتفاع، هر `CORE_HALF_STEP` پیکسل. */
  readonly coreHalf: readonly number[];
}

type Node = readonly [dx: number, y: number];

/**
 * نیمهٔ راستِ سیلوئت: از تارک (روی محور) تا فاق (روی محور)، ساعت‌گرد.
 * `dx` فاصله از محورِ تقارن است.
 * بازوها در حالت آناتومی (کنارِ تنه) رسم شده‌اند، نه حالت A/T.
 */
const HALF_OUTLINE: readonly Node[] = [
  // نوکِ تارک کمی بالاتر از همسایه‌اش نشسته تا آینه‌شدن، بالای سر را گود نکند.
  [0, 5.6], [3.66, 5.95], [8.18, 6.47], [17.43, 11.97],
  [19.94, 14.38], [24.21, 19.88], [26.69, 25.88], [27.18, 33.13],
  [26.52, 41.88], [25.58, 47.13], [27.48, 52.63], [27.39, 55.88],
  [25.91, 61.63], [24.58, 64.63], [23.33, 66.14], [21.04, 67.16],
  [20.19, 68.14], [18, 78.38], [18.31, 87.63], [19.08, 91.88],
  [20.21, 94.63], [22.68, 97.39], [27.43, 100.45],
  // شانه — شروع بازو (حالت آناتومی: بازو به‌لبه‌ی تنه می‌چسبد)
  [28.5, 108.07], [29.5, 116.38], [30.2, 124.38], [30.8, 133.63],
  [31.2, 143.63], [31.5, 155.88], [31.6, 168.13], [31.5, 179.88],
  [31.2, 191.63], [30.8, 203.13], [30.2, 214.13], [29.5, 227.88],
  [28.5, 237.13], [27.8, 249.88], [27.5, 262.13], [27.8, 274.88],
  [28.5, 282.88],
  // کمر و لگن — بازو به‌لبه‌ی تنه می‌رسد
  [32.5, 309.13], [36.5, 311.61], [42.5, 320.13], [49.5, 331.13],
  [52.5, 334.05], [53.5, 335.34], [52.5, 336.55], [49.5, 336.16],
  [46.5, 334.24], [43.5, 330.99], [42.5, 330.86], [42.5, 334.13],
  [44.5, 348.13], [44.5, 358.38], [43.5, 359.93], [42.5, 360.57],
  [41.5, 360.13], [40.5, 358.88], [39.5, 346.88], [38.5, 341.64],
  [38.5, 341.11], [39.5, 349.63], [39.5, 362.38], [39.5, 364.09],
  [38.5, 365.28], [37.5, 365.2], [36.5, 363.38], [36.5, 351.63],
  [35.5, 342.45], [34.5, 361.63], [33.5, 363.41], [32.5, 363.86],
  [31.5, 362.13], [31.5, 341.38], [31.5, 339.86], [31.5, 340.63],
  [30.5, 354.86], [29.5, 356.18], [28.5, 355.84], [28.5, 354.88],
  [28.5, 352.38], [28.5, 337.88], [26.5, 325.38], [26.5, 312.63],
  [22.5, 269.38], [18.5, 234.63], [17.5, 231.63], [17.5, 220.63],
  [15.5, 204.38], [14.5, 191.13], [14.5, 190.43], [14.5, 191.38],
  [12.5, 210.63], [12.5, 218.63], [12.5, 225.38], [15.5, 245.38],
  [16.5, 265.38], [18.5, 270.63], [20.5, 288.13], [22.5, 313.38],
  [21.5, 326.88], [18.5, 380.88], [17.5, 391.13], [16.5, 399.38],
  [15.5, 409.63], [15.5, 425.38], [18.5, 446.63], [19.5, 458.88],
  [18.5, 525.88], [18.5, 528.88], [19.5, 533.88], [20.5, 543.63],
  [24.5, 556.13], [30.5, 569.88], [31.5, 572.38], [31.5, 575.63],
  [31.5, 580.13], [29.5, 583.63], [28.5, 586.55], [27.5, 588.32],
  [26.5, 590.18], [23.5, 593.32], [21.5, 593.39], [19.5, 594.89],
  [17.5, 595.38], [15.5, 594.59], [13.5, 591.88], [11.5, 582.63],
  [10.5, 567.63], [8.5, 558.63], [7.5, 551.13], [5.5, 538.13],
  [5.5, 535.88], [6.5, 530.38], [6.5, 523.88], [5.5, 509.88],
  [3.5, 491.63], [1.5, 474.88], [1.5, 469.38], [0.5, 435.13],
  [0, 421.88], [0, 414.13], [0, 384.63], [0, 343.13],
  [0, 342.34], [0, 342.03], [0, 343.26],
];

/**
 * نیم‌پهنای «تنه» (بدونِ بازوها) در هر ارتفاع، هر ۴ پیکسل یک نمونه.
 *
 * از همان رسترِ متقارن به دست آمده: در هر سطر، بازه‌ای که محورِ تقارن را در بر
 * می‌گیرد. موتورِ ریخت‌دهی با این جدول می‌فهمد کجا تنه تمام و بازو شروع می‌شود،
 * تا چاق‌شدنِ تنه بازوها را کش ندهد و فقط کنارشان بزند.
 * ۱۵۱ مقدار برای y=0..600 با گام ۴.
 */
const CORE_HALF_MALE: readonly number[] = [
  0, 0, 11.1, 17.9, 21.5, 24.5, 26.4, 27.1, 27.3, 27.1, 26.8, 26.3,
  25.6, 27.5, 27.5, 26.5, 25, 20.2, 19.4, 18.6, 18, 18.2, 18.5, 19.3,
  21.4, 27.1, 28.5, 29.2, 29.8, 30.3, 30.7, 31.0, 31.2, 31.3, 31.4, 31.4,
  31.4, 31.4, 31.4, 31.4, 31.3, 31.2, 31.0, 30.7, 30.3, 29.8, 29.2, 28.5,
  28.0, 27.8, 27.6, 27.5, 27.6, 28.0, 28.5, 32.5, 35.5, 41.0, 47.5,
  51.0, 52.5, 51.5, 48.5, 45.5, 42.5, 42.5, 43.5, 43.5, 42.5, 41.5, 40.5,
  31.5, 31.5, 31.5, 31.5, 30.5, 30.0, 29.5, 28.5, 28.5, 28.5, 26.5, 24.5,
  22.5, 20.5, 18.5, 16.5, 15.5, 14.5, 14.5, 14.5, 12.5, 12.5, 12.5, 14.5,
  15.5, 17.5, 18.5, 20.5, 22.5, 21.5, 18.5, 17.5, 16.5, 15.5, 15.5, 15.5,
  18.5, 19.5, 18.5, 18.5, 18.5, 19.5, 20.5, 22.5, 23.5, 25.5, 26.5, 27.5,
  28.5, 29.5, 29.5, 30.5, 30.5, 30.5, 28.5, 27.5, 26.5, 24.5, 22.5, 20.5,
  18.5, 16.5, 14.5, 12.5, 10.5, 8.5, 6.5, 5.5, 5.5, 6.5, 6.5, 5.5,
  3.5, 1.5, 1.5, 0.5, 0, 0, 0, 0,
];

/**
 * تفاوتِ نسبت‌های زنانه، به‌صورت ضریبِ پهنا بر حسب ارتفاع.
 *
 * چرا این‌جا و نه در موتورِ وارپ: تفاوتِ زن و مرد یک ویژگیِ «شکلِ مرجع» است نه
 * یک تغییرِ ناشی از قد و وزن. با آوردنش به این‌جا، ضرایبِ چاقی/لاغری روی هر دو
 * پیکره یکسان عمل می‌کنند و نتیجه پیش‌بینی‌پذیر می‌ماند.
 */
const FEMALE_WIDTH: readonly Node[] = [
  [0.95, 0],
  [0.955, 70],
  [0.93, 95],
  [0.875, 112],
  [0.895, 150],
  [0.925, 185],
  [0.95, 205],
  [0.865, 250],
  [1.045, 285],
  [1.145, 315],
  [1.135, 345],
  [1.075, 400],
  [0.995, 455],
  [0.955, 520],
  [0.93, 600],
];

/** برجستگیِ سینه — فقط روی لبهٔ تنه، نه روی بازویی که هم‌ارتفاعِ آن است. */
const BUST = { center: 196, spread: 26, amount: 5 } as const;

// ─── ابزارِ عددی ─────────────────────────────────────────────────────────────

const at = (values: readonly number[], index: number): number => values[index] as number;

/** درون‌یابیِ خطیِ جدولِ [مقدار, ارتفاع] که بر حسب ارتفاع مرتب است. */
function sampler(nodes: readonly Node[]): (y: number) => number {
  const last = nodes.length - 1;
  return (y: number): number => {
    const first = nodes[0] as Node;
    if (y <= first[1]) return first[0];
    const tail = nodes[last] as Node;
    if (y >= tail[1]) return tail[0];
    for (let i = 0; i < last; i += 1) {
      const a = nodes[i] as Node;
      const b = nodes[i + 1] as Node;
      if (y <= b[1]) return a[0] + ((b[0] - a[0]) * (y - a[1])) / (b[1] - a[1]);
    }
    return tail[0];
  };
}

/**
 * اسپلاینِ کاتمول-رومِ «مرکزگرا» (alpha = ۰٫۵) روی یک حلقهٔ بسته.
 *
 * چرا مرکزگرا: نسخهٔ یکنواخت وقتی فاصلهٔ نقاط ناهمگون است (مثل نوکِ انگشتان
 * کنارِ ساعدِ بلند) حلقه و گره می‌زند. مرکزگرا تضمین می‌کند منحنی از خودش
 * عبور نکند — دقیقاً چیزی که برای یک سیلوئتِ بسته لازم است.
 */
function closedCatmullRom(points: readonly (readonly [number, number])[]): string {
  const n = points.length;
  const get = (i: number) => points[((i % n) + n) % n] as readonly [number, number];
  const dist = (a: readonly [number, number], b: readonly [number, number]) =>
    Math.max(Math.hypot(b[0] - a[0], b[1] - a[1]), 1e-6) ** 0.5;

  const fmt = (value: number) => value.toFixed(2);
  const first = get(0);
  let d = `M${fmt(first[0])},${fmt(first[1])}`;

  for (let i = 0; i < n; i += 1) {
    const p0 = get(i - 1);
    const p1 = get(i);
    const p2 = get(i + 1);
    const p3 = get(i + 2);

    const d1 = dist(p0, p1);
    const d2 = dist(p1, p2);
    const d3 = dist(p2, p3);

    // نقاطِ کنترل از مماسِ کاتمول-رومِ ناهم‌فاصله؛ ضریبِ ۱/۳ همان تبدیلِ
    // استانداردِ هرمیت به بزیهٔ مکعبی است.
    const k1 = d2 / (3 * (d1 + d2));
    const k2 = d2 / (3 * (d2 + d3));

    const c1: [number, number] = [p1[0] + (p2[0] - p0[0]) * k1, p1[1] + (p2[1] - p0[1]) * k1];
    const c2: [number, number] = [p2[0] - (p3[0] - p1[0]) * k2, p2[1] - (p3[1] - p1[1]) * k2];

    d += `C${fmt(c1[0])},${fmt(c1[1])} ${fmt(c2[0])},${fmt(c2[1])} ${fmt(p2[0])},${fmt(p2[1])}`;
  }

  return `${d}Z`;
}

// ─── ساختِ سیلوئت ────────────────────────────────────────────────────────────

/** نیم‌پهنای تنه در ارتفاعِ `y` روی جدولِ مردانه (درون‌یابیِ خطی). */
function maleCoreHalf(y: number): number {
  const position = y / CORE_HALF_STEP;
  const index = Math.floor(position);
  if (index < 0) return at(CORE_HALF_MALE, 0);
  if (index >= CORE_HALF_MALE.length - 1) return at(CORE_HALF_MALE, CORE_HALF_MALE.length - 1);
  const t = position - index;
  return at(CORE_HALF_MALE, index) * (1 - t) + at(CORE_HALF_MALE, index + 1) * t;
}

function buildOutline(sex: BodySex): BaseOutline {
  const femaleScale = sampler(FEMALE_WIDTH);

  /**
   * برجستگیِ سینه فقط به نقاطی اضافه می‌شود که روی لبهٔ تنه‌اند؛ نقاطِ بازو در
   * همان ارتفاع باید دست‌نخورده بمانند، وگرنه بازو هم باد می‌کند.
   */
  const shape = (dx: number, y: number): number => {
    if (sex !== "female") return dx;
    const scaled = dx * femaleScale(y);
    if (dx > maleCoreHalf(y) + 2) return scaled;
    return scaled + BUST.amount * Math.exp(-(((y - BUST.center) / BUST.spread) ** 2));
  };

  const right = HALF_OUTLINE.map(([dx, y]): [number, number] => [CENTER_X + shape(dx, y), y]);

  // آینه: نیمهٔ چپ دقیقاً همان نقاط است با علامتِ معکوس. تارک و فاق روی محورند
  // و تکرار نمی‌شوند تا حلقه گره نخورد.
  const mirrored = right
    .slice(1, right.length - 1)
    .reverse()
    .map(([x, y]): [number, number] => [CENTER_X - (x - CENTER_X), y]);

  const coreHalf = CORE_HALF_MALE.map((value, index) =>
    sex === "female" ? value * femaleScale(index * CORE_HALF_STEP) : value,
  );

  return { path: closedCatmullRom([...right, ...mirrored]), coreHalf };
}

const cache = new Map<BodySex, BaseOutline>();

/** سیلوئتِ مرجعِ متقارن برای یک جنسیت (کش‌شده). */
export function getBaseOutline(sex: BodySex): BaseOutline {
  const cached = cache.get(sex);
  if (cached) return cached;
  const built = buildOutline(sex);
  cache.set(sex, built);
  return built;
}

/** نیم‌پهنای تنه در ارتفاعِ `y` با درون‌یابیِ جدول. */
export function coreHalfFrom(table: readonly number[], y: number): number {
  const position = y / CORE_HALF_STEP;
  const index = Math.floor(position);
  if (index < 0) return at(table, 0);
  if (index >= table.length - 1) return at(table, table.length - 1);
  const t = position - index;
  return at(table, index) * (1 - t) + at(table, index + 1) * t;
}
