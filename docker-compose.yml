# Usage:  cp .env.example .env  &&  docker compose up -d --build
#
# Host ports are deliberately unusual so they never collide with anything else
# on the machine, and are bound to 127.0.0.1 only (not reachable from outside):
#   frontend 47391   backend 47392   MariaDB 47393
# Local:   http://localhost:47391  (app)   http://localhost:47392  (API)
# Server:  the host nginx reverse-proxies
#          https://form.dayins.ir/health-analysis/ -> frontend and
#          https://form.dayins.ir/health-analysis/api/ -> backend.
#          See deploy/nginx-form.dayins.ir.conf.
services:
  db:
    image: mariadb:11
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: health_survey
      MYSQL_USER: health_app
      MYSQL_PASSWORD: ${DB_APP_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    ports:
      - "${BIND_HOST:-127.0.0.1}:${DB_PORT:-47393}:3306"

  backend:
    build:
      context: ./backend
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-}
    restart: unless-stopped
    environment:
      DATABASE_URL: mysql+pymysql://health_app:${DB_APP_PASSWORD}@db:3306/health_survey
      CORS_ALLOW_ORIGINS: ${CORS_ALLOW_ORIGINS:-http://localhost:47391,http://127.0.0.1:47391}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${BIND_HOST:-127.0.0.1}:${BACKEND_PORT:-47392}:8000"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:47392}
        VITE_BASE_PATH: ${VITE_BASE_PATH:-/}
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "${BIND_HOST:-127.0.0.1}:${FRONTEND_PORT:-47391}:80"

volumes:
  db_data:
